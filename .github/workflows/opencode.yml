# Codex CLI — Issue 触发自动改代码
# 模型：gpt-5.2
name: Codex Agent

on:
  issues:
    types: [opened, labeled]
  pull_request:
    types: [closed]

permissions:
  contents: write
  pull-requests: write
  issues: write
  actions: write

jobs:
  opencode:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    concurrency:
      group: opencode-issue-${{ github.event.issue.number }}
      cancel-in-progress: true
    # 当 issue 被打开，或者被标记为 "auto-work" 时触发
    if: |
      (github.event_name == 'issues' && github.event.action == 'opened') ||
      (github.event_name == 'issues' && github.event.label.name == 'auto-work')
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Notify Telegram (issue started)
        if: github.event_name == 'issues'
        env:
          TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
          ISSUE_NUM: ${{ github.event.issue.number }}
          ISSUE_TITLE: ${{ github.event.issue.title }}
        run: |
          if [ -z "$TG_BOT_TOKEN" ] || [ -z "$TG_CHAT_ID" ]; then
            echo "TG secrets missing, skip notification"
            exit 0
          fi
          ISSUE_URL="${{ github.server_url }}/${{ github.repository }}/issues/${ISSUE_NUM}"
          RUN_URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          TEXT=$(cat <<EOF
          🚀 [Issue开始处理] #${ISSUE_NUM} ${ISSUE_TITLE}
          ${ISSUE_URL}
          Run: ${RUN_URL}
          EOF
          )
          curl -fsS -X POST "https://api.telegram.org/bot${TG_BOT_TOKEN}/sendMessage" \
            -d chat_id="$TG_CHAT_ID" \
            --data-urlencode text="$TEXT" \
            -d disable_web_page_preview=true >/dev/null || true

      - name: Configure Git
        run: |
          git config user.name "codex[bot]"
          git config user.email "codex[bot]@users.noreply.github.com"

      - name: Install Codex CLI
        run: npm install -g @openai/codex

      - name: Collect PR reviewer feedback (latest)
        id: feedback
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const issueNum = context.payload.issue.number;
            const marker = '<!-- AI_PR_REVIEW_FEEDBACK -->';
            const comments = await github.paginate(github.rest.issues.listComments, {
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNum,
              per_page: 100,
            });
            const rounds = comments.filter(c => (c.body || '').includes(marker)).length;
            const latest = [...comments].reverse().find(c => (c.body || '').includes(marker));
            core.setOutput('rounds', String(rounds));
            core.setOutput('text', latest ? latest.body : '');

      - name: Build Codex prompt
        env:
          ISSUE_TITLE: ${{ github.event.issue.title }}
          ISSUE_BODY: ${{ github.event.issue.body }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          REVIEW_FEEDBACK: ${{ steps.feedback.outputs.text }}
          REVIEW_ROUNDS: ${{ steps.feedback.outputs.rounds }}
        run: |
          python3 - <<'PY'
          import os
          from pathlib import Path

          title = os.environ.get('ISSUE_TITLE','')
          body = os.environ.get('ISSUE_BODY','')
          num = os.environ.get('ISSUE_NUMBER','')
          feedback = os.environ.get('REVIEW_FEEDBACK','').strip()
          rounds = os.environ.get('REVIEW_ROUNDS','0')

          parts = []
          parts.append('根据以下需求修改代码：')
          parts.append(f'标题：{title}')
          parts.append('内容：')
          parts.append(body)
          parts.append('')
          parts.append('RULES:')
          parts.append('1. ONLY modify files listed in the issue\'s \'Scope\' section (if any)')
          parts.append('2. Focus on: correctness, performance, robustness, clean code')
          parts.append('3. 禁止读取/遍历 *.test.ts、docs、node_modules；只处理 scope 文件')
          parts.append('4. Run \'bun test\' after changes to verify nothing breaks')
          parts.append('5. Keep changes minimal and focused on the issue description')
          parts.append(f'6. Commit with message: \'fix: <short description> (closes #{num})\'')

          if feedback:
            parts.append('')
            parts.append('---')
            parts.append(f'PR Reviewer 反馈（累计 {rounds} 轮；如有 Blocking 必须优先解决）：')
            parts.append(feedback)

          Path('/tmp/codex_prompt.txt').write_text('\n'.join(parts), encoding='utf-8')
          PY

      - name: Run Codex
        env:
          CUSTOM_OPENAI_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          timeout 45m codex \
            -c 'model_providers.custom_openai.name="Custom OpenAI"' \
            -c 'model_providers.custom_openai.base_url="${{ secrets.OPENAI_BASE_URL }}"' \
            -c 'model_providers.custom_openai.env_key="CUSTOM_OPENAI_KEY"' \
            -c 'model_provider="custom_openai"' \
            exec --full-auto -m gpt-5.2 \
            - < /tmp/codex_prompt.txt

      - name: Create PR
        id: pr
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.PAT }}
          branch: codex/issue-${{ github.event.issue.number }}
          title: "🤖 Codex: ${{ github.event.issue.title }}"
          body: |
            由 Codex CLI (gpt-5.2) 自动生成
            对应 Issue #${{ github.event.issue.number }}
            
            Closes #${{ github.event.issue.number }}
          commit-message: "feat: codex auto-fix #${{ github.event.issue.number }}"
          committer: Codex <codex@users.noreply.github.com>
          author: Codex <codex@users.noreply.github.com>
          labels: agent-codex,auto-pr

      - name: Comment on issue
        if: steps.pr.outputs.pull-request-number
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const issueNum = ${{ github.event.issue.number }};
            const prNum = ${{ steps.pr.outputs.pull-request-number }};
            const msg = `✅ Codex 已创建 PR #${prNum}\n\n请查看并合并。`;

            const comments = await github.paginate(github.rest.issues.listComments, {
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNum,
              per_page: 100,
            });

            const already = comments.some(c => (c.body || '').includes(`PR #${prNum}`));
            if (already) {
              core.info('Comment already exists, skip');
              return;
            }

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNum,
              body: msg,
            });

      - name: Notify Telegram (PR created)
        if: steps.pr.outputs.pull-request-number
        env:
          TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
          ISSUE_NUM: ${{ github.event.issue.number }}
          PR_NUM: ${{ steps.pr.outputs.pull-request-number }}
        run: |
          if [ -z "$TG_BOT_TOKEN" ] || [ -z "$TG_CHAT_ID" ]; then
            echo "TG secrets missing, skip notification"
            exit 0
          fi
          ISSUE_URL="${{ github.server_url }}/${{ github.repository }}/issues/${ISSUE_NUM}"
          PR_URL="${{ github.server_url }}/${{ github.repository }}/pull/${PR_NUM}"
          TEXT=$(cat <<EOF
          🧩 [PR已创建] #${PR_NUM} (from Issue #${ISSUE_NUM})
          ${PR_URL}
          Issue: ${ISSUE_URL}
          EOF
          )
          curl -fsS -X POST "https://api.telegram.org/bot${TG_BOT_TOKEN}/sendMessage" \
            -d chat_id="$TG_CHAT_ID" \
            --data-urlencode text="$TEXT" \
            -d disable_web_page_preview=true >/dev/null || true

      - name: Notify Telegram (issue run failed/cancelled)
        if: failure() || cancelled()
        env:
          TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
          ISSUE_NUM: ${{ github.event.issue.number }}
          JOB_STATUS: ${{ job.status }}
        run: |
          if [ -z "$TG_BOT_TOKEN" ] || [ -z "$TG_CHAT_ID" ]; then
            exit 0
          fi
          ISSUE_URL="${{ github.server_url }}/${{ github.repository }}/issues/${ISSUE_NUM}"
          RUN_URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          TEXT=$(cat <<EOF
          ❌ [Issue处理异常] #${ISSUE_NUM} (${JOB_STATUS})
          ${ISSUE_URL}
          Run: ${RUN_URL}
          EOF
          )
          curl -fsS -X POST "https://api.telegram.org/bot${TG_BOT_TOKEN}/sendMessage" \
            -d chat_id="$TG_CHAT_ID" \
            --data-urlencode text="$TEXT" \
            -d disable_web_page_preview=true >/dev/null || true

  # 链式触发：当 PR 合并后，触发下一个 issue
  chain-next:
    runs-on: ubuntu-latest
    if: |
      github.event_name == 'pull_request' &&
      github.event.pull_request.merged == true &&
      contains(github.event.pull_request.labels.*.name, 'auto-pr')
    steps:
      - uses: actions/checkout@v4

      - name: Find and trigger next issue
        id: chain
        env:
          # 用 PAT 添加 label，确保能触发 issues:labeled（GITHUB_TOKEN 会被 GitHub 防递归吞掉）
          GH_TOKEN: ${{ secrets.PAT }}
          PR_BODY: ${{ github.event.pull_request.body }}
        run: |
          CLOSED_ISSUE=$(echo "$PR_BODY" | grep -oP 'Closes #\K\d+' | head -1)
          
          if [ -z "$CLOSED_ISSUE" ]; then
            echo "No linked issue found, skipping chain"
            echo "closed_issue=" >> $GITHUB_OUTPUT
            echo "next_issues=" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          echo "Merged PR closed issue #$CLOSED_ISSUE"
          
          # Find next issue in the chain
          NEXT_ISSUES=$(gh issue list --state open --label "auto-chain" --json number,body -q ".[] | select(.body | test(\"depends on #$CLOSED_ISSUE\"; \"i\")) | .number" | tr '\n' ' ' | xargs)
          
          for ISSUE_NUM in $NEXT_ISSUES; do
            echo "Triggering next issue: #$ISSUE_NUM"
            gh issue edit "$ISSUE_NUM" --add-label "auto-work"
          done
          
          if [ -z "$NEXT_ISSUES" ]; then
            echo "No chained issues found. Pipeline complete!"
          fi

          echo "closed_issue=$CLOSED_ISSUE" >> $GITHUB_OUTPUT
          echo "next_issues=$NEXT_ISSUES" >> $GITHUB_OUTPUT

      - name: Notify Telegram (merged / chain status, detailed)
        env:
          TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
          PR_NUM: ${{ github.event.pull_request.number }}
          CLOSED_ISSUE: ${{ steps.chain.outputs.closed_issue }}
          NEXT_ISSUES: ${{ steps.chain.outputs.next_issues }}
        run: |
          if [ -z "$TG_BOT_TOKEN" ] || [ -z "$TG_CHAT_ID" ]; then
            exit 0
          fi
          PR_URL="${{ github.server_url }}/${{ github.repository }}/pull/${PR_NUM}"
          if [ -n "$NEXT_ISSUES" ]; then
            TEXT=$(cat <<EOF
            ✅ [已合并-链路继续]
            PR #${PR_NUM} 已合并
            ${PR_URL}
            关闭 Issue: #${CLOSED_ISSUE}
            已触发下一个: ${NEXT_ISSUES}
            EOF
            )
          else
            TEXT=$(cat <<EOF
            ✅ [已合并-链路完成]
            PR #${PR_NUM} 已合并
            ${PR_URL}
            关闭 Issue: #${CLOSED_ISSUE}
            没有下一个 auto-chain 任务。
            EOF
            )
          fi
          curl -fsS -X POST "https://api.telegram.org/bot${TG_BOT_TOKEN}/sendMessage" \
            -d chat_id="$TG_CHAT_ID" \
            --data-urlencode text="$TEXT" \
            -d disable_web_page_preview=true >/dev/null || true
