# QMDR 环境变量配置
# 复制为 .env 并填入你的 API Key，然后 source .env
#
# 💡 三种配置方式（任选其一）：
#
# 方式一：只用 SiliconFlow（最简，一个 Key）
#   → Embedding/Expansion/Rerank 全走 SiliconFlow
#
# 方式二：SiliconFlow + Gemini（推荐，各取所长）
#   → Embedding/Expansion 走 SiliconFlow，Rerank 走 Gemini
#
# 方式三：只用 OpenAI 兼容 API
#   → 支持火山引擎（豆包免费）、各种中转站等

# ============================================================
# 方式一：SiliconFlow（https://siliconflow.cn 注册即有免费额度）
# ============================================================

QMD_SILICONFLOW_API_KEY=sk-your-siliconflow-key-here

# ============================================================
# 方式二：加上 Gemini（https://ai.google.dev 免费额度）
# ============================================================

# QMD_GEMINI_API_KEY=your-gemini-key-here
# QMD_GEMINI_MODEL=gemini-2.5-flash

# ============================================================
# 方式三：OpenAI 兼容 API
# 支持：火山引擎、OpenAI、Azure、各种中转站
# ============================================================

# QMD_OPENAI_API_KEY=your-key-here
# QMD_OPENAI_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
# QMD_OPENAI_MODEL=doubao-1.5-pro
# QMD_OPENAI_EMBED_MODEL=doubao-embedding

# ============================================================
# 可选：手动指定 Provider（通常不需要，设 Key 会自动推断）
# ============================================================

# QMD_EMBED_PROVIDER=siliconflow
# QMD_QUERY_EXPANSION_PROVIDER=siliconflow
# QMD_RERANK_PROVIDER=openai

# ============================================================
# 可选：自定义模型（有合理默认值）
# ============================================================

# SiliconFlow 模型
# QMD_SILICONFLOW_EMBED_MODEL=Qwen/Qwen3-Embedding-8B        # Embedding 模型（4096 维）
# QMD_SILICONFLOW_QUERY_EXPANSION_MODEL=zai-org/GLM-4.5-Air   # 查询扩展模型
# QMD_SILICONFLOW_RERANK_MODEL=BAAI/bge-reranker-v2-m3        # 重排序模型（SiliconFlow 模式）

# Gemini 模型
# QMD_GEMINI_MODEL=gemini-2.5-flash                           # Gemini 重排序模型
# QMD_GEMINI_BASE_URL=https://generativelanguage.googleapis.com

# OpenAI 兼容模型
# QMD_OPENAI_MODEL=gpt-4o-mini                                # 重排序/扩展模型
# QMD_OPENAI_EMBED_MODEL=text-embedding-3-small               # Embedding 模型
# QMD_OPENAI_BASE_URL=https://api.openai.com/v1

# ============================================================
# 可选：性能调优（有合理默认值）
# ============================================================

# QMD_CHUNK_SIZE_TOKENS=200           # 每个 chunk 的 token 数（默认 200）
# QMD_CHUNK_OVERLAP_TOKENS=40         # chunk 间重叠 token 数（默认 40）
# QMD_EMBED_BATCH_SIZE=32             # Embedding 批处理大小（默认 32）
# QMD_RERANK_DOC_LIMIT=40             # Rerank 最大文档数（默认 40）
# QMD_RERANK_CHUNKS_PER_DOC=3         # 每文档参与 Rerank 的 chunk 数（默认 3）
# QMD_SQLITE_VEC_PATH=                # sqlite-vec 动态库路径（自动检测）
# QMD_LAUNCHD_PLIST=                  # doctor 用的 launchd plist 路径（自动检测）

# ============================================================
# 通用
# ============================================================

# XDG_CACHE_HOME=~/.cache
